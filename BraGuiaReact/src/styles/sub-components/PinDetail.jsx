import { StyleSheet } from 'react-native';

const PinDetailStyles = (color) => StyleSheet.create({
  container: {
    flexGrow: 1,
    padding: 16,
    backgroundColor: color
  },
});

export default PinDetailStyles;
